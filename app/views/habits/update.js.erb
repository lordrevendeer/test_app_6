var followButton = document.getElementById('completion');

if (followButton.innerText == 'Habit completed') {
  followButton.innerText = 'Habit done for today';
} 
 else {
  followButton.innerText = 'Habit completed';
}