var followButton = document.getElementById('completion');

if (followButton.innerText == 'Habit done today') {
  followButton.innerText = 'Well done!';
} 
 else {
  followButton.innerText = 'Habit done today';
}
